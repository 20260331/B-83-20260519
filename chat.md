# 改造应用首页为业务工作台

> **Workspace:** d:\code\trae\gsb\20260518\B-83-代码生成-1\TestM_3

---

## User
把当前应用首页改造成一个可实际使用的业务工作台，首页要展示核心数据概览、待办/最近记录和常用功能入口，要能直接在首页完成新增、查询、导入等高频操作。

## Assistant
构建成功。现在启动开发服务器，让你可以预览改造后的工作台效果。


服务已启动，运行在 http://localhost:5173/。下面是本次改造的完整说明：

## 改造完成 ✅

将原有的默认页面改造成了一个**企业业务工作台**，核心改动如下：

### 文件变更
- `src/App.vue` — 移除模板内容，改为引入 `Workbench.vue`
- `src/style.css` — 重置全局样式，使用浅色办公风主题色
- `src/components/HelloWorld.vue` — 已被 `Workbench.vue` 替代
- `src/components/Workbench.vue` — **新建**，整页工作台组件
- 新增依赖 `@element-plus/icons-vue`

### 工作台三大视图

1. **数据概览（首页）**
   - **核心数据卡片**：今日新增客户 / 待处理订单 / 本月销售额 / 异常预警，每张卡片带图标、趋势增减色
   - **常用功能入口**：新增客户、新增订单、数据查询、批量导入、数据导出、报表分析、系统设置、消息中心（带待办数角标）
   - **待办事项**：可勾选完成，完成后划线+置灰
   - **最近记录**：实时更新的业务操作流水

2. **业务查询**
   - 关键词 + 类型 + 状态 多维筛选
   - 结果表格带状态标签、金额格式化、操作列（详情/编辑/删除）
   - 底部按钮组可直接在查询页执行新增/导入

3. **统计分析**（预留扩展区，后续接入图表库即可）

### 高频操作闭环（均在首页可完成）
| 操作 | 入口 | 效果 |
|---|---|---|
| ➕ 新增客户 | 首页快捷入口 / 查询页 | 弹窗表单 → 记录入列表 → 自动生成跟进待办 |
| 📝 新增订单 | 首页快捷入口 / 查询页 | 弹窗表单 → 记录入列表 → 待办数自动 +1 |
| 📥 批量导入 | 首页快捷入口 / 查询页 | 拖拽上传 .xlsx/.xls/.csv → 类型选择 → 导入反馈 |
| 🔍 数据查询 | 首页快捷入口 / 顶部 Tab | 多条件筛选 + 结果高亮 |
| 📤 数据导出 | 查询页 | 一键导出当前筛选结果 |
| ✅ 完成待办 | 待办区勾选 | 即时状态切换 + 提示 |

### 技术要点
- Vue 3 + `<script setup lang="ts">`，所有业务类型均定义了 TS 接口
- Element Plus 表单、对话框、表格、上传组件完整覆盖
- 状态使用 `ref`/`reactive`/`computed` 管理，无外部状态库依赖
- 响应式布局：宽屏 4 列统计卡片 / 8 列快捷入口，窄屏自动降级为 2/1 列

